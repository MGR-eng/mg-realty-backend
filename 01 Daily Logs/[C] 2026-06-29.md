---
author: claude
type: daily
date: 2026-06-29
---

# Session Log — Monday, June 29 2026

## What We Worked On
- Budget tracker — connected to CRM backend (replaced localStorage with API calls)
- Budget tracker — added month-by-month navigation clarity + PDF export button
- Budget tracker — added to CRM sidebar under Finance as a live link
- Facebook cross-posting — built OAuth flow, attempted to wire up FB Page token
- Facebook cross-posting — hit Meta API scope walls; decided to use native Instagram → Facebook sharing instead

## What Was Built or Changed
- `personal-tools/budget-tracker.html` — full rewrite: pulls from `/api/budget-expenses`, month nav, PDF export, Ace receipt badge, category mapping
- `public/budget-tracker.html` — copy deployed to public folder so CRM sidebar link works
- `public/index.html` — added "Budget Tracker" link under Finance nav, added Facebook cross-post checkbox + permalink row to Instagram publisher
- `server.js` — added `readFBCreds()`, `saveFBCreds()`, `/auth/facebook`, `/auth/facebook/callback`, `/auth/facebook/select`, `/auth/facebook/from-instagram` routes; added `postToFacebook()` helper; updated `/api/post-instagram` and `/api/ig-poll` to support FB cross-posting when creds are present

## Still Open
- Facebook cross-posting via API — shelved for now; use Instagram's native "Share to Facebook" setting in Instagram app → Settings → Account → Sharing to other apps
- CLAW MLS API — called (310) 358-1100, no callback yet; follow up again
- `FB_APP_ID` and `FB_APP_SECRET` are set in Render but FB auth flow is blocked by Meta scope issues — revisit when there's bandwidth to create a clean separate Facebook app

## Start Here Tomorrow
Enable native Instagram → Facebook sharing in Instagram settings (Settings → Account → Sharing to other apps → Facebook) to get cross-posting working without the API. Then follow up with CLAW at (310) 358-1100 for MLS data access.
